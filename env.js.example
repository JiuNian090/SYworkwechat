/**
 * 环境变量配置读取模块 - 示例文件
 * 请复制此文件为 utils/env.js 并填入你的真实配置
 * utils/env.js 文件已添加到 .gitignore，不会被提交到git仓库
 */

const envConfig = {
  // 小程序AppID
  APP_ID: 'your_appid_here',
  
  // 云环境ID
  CLOUD_ENV: 'your_cloud_env_here'
};

module.exports = envConfig;
