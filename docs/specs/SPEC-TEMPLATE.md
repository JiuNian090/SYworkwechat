# SPEC-XXX 【需求名称】

## 一、核心目标（对应 Goal-Driven Execution）
【一句话写清需求要解决的业务问题，可量化的最终效果，禁止模糊描述】

## 二、明确假设与边界（对应 Think Before Coding）
### 明确假设
【列出所有需求相关的明确假设，不确定的内容必须标注，禁止 AI 静默假设】
1.  假设1：
2.  假设2：

### 歧义澄清
【列出需求中可能存在歧义的内容，明确澄清，避免 AI 错误理解】
1.
2.

## 三、范围边界（对应 Simplicity First）
### 纳入范围（必须实现）
【明确列出必须实现的功能点，精确到页面/组件/接口】
1.
2.

### 排除范围（绝对禁止实现）
【明确列出不需要实现的内容，从根源避免过度工程】
1.
2.

## 四、可验证验收标准（对应 Goal-Driven Execution）
【每个标准必须可执行、可验证，禁止模糊描述，每个标准都有明确的验证方式】
1.  【验收标准1】→ 验证方式：【怎么检查是否通过，比如：微信开发者工具编译无报错，页面正常跳转，功能正常执行】
2.  【验收标准2】→ 验证方式：【ESLint 校验无报错，代码符合项目规范】
3.  【验收标准3】→ 验证方式：【对应测试用例全部通过】

## 五、代码修改规则（对应 Surgical Changes）
1.  仅允许修改的目录/文件：【明确列出允许修改的目录、文件，比如：pages/schedule/、components/xx/】
2.  绝对禁止修改的目录/文件：【明确列出禁止修改的核心文件，比如：app.json、app.js、utils/encrypt.js】
3.  必须遵循的代码风格：【匹配项目现有风格，遵循 ESLint 严格模式】

## 六、项目适配要求
1.  新增页面必须在 app.json 中注册，新增组件必须在对应页面 json 中注册
2.  所有代码必须通过 ESLint 校验，无警告、无报错
3.  功能变更必须同步更新 CHANGELOG.md，遵循语义化版本规范
4.  涉及用户数据的操作必须遵循项目已有的加密规范
