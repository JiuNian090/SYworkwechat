/* pages/schedule/schedule.wxss */

/* CSS Variables - 统一设计系统 */
page {
  --primary-color: #34d399;
  --primary-light: #6ee7b7;
  --primary-dark: #10b981;
  --bg-color: #f5f5f5;
  --card-bg: #ffffff;
  --text-primary: #333333;
  --text-secondary: #666666;
  --text-tertiary: #999999;
  --border-color: #e5e5e5;
  --shadow-color: rgba(0, 0, 0, 0.08);
  --day-shift-color: #fbbf24;
  --night-shift-color: #6366f1;
  --off-day-color: #999999;
  --success-color: #34d399;
  --warning-color: #fbbf24;
  --error-color: #f87171;
  
  background-color: var(--bg-color);
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Helvetica Neue', Arial, sans-serif;
}

/* 页面容器 */
.container {
  min-height: 100vh;
  padding-bottom: 40rpx;
  opacity: 1;
  transition: opacity 0.2s ease-in-out;
}

/* 页面标题区 - 与profile页面头像标题区一致 */
.page-header {
  position: relative;
  margin: 30rpx 30rpx 30rpx;
  padding: 65rpx 30rpx 65rpx;
  background: linear-gradient(135deg, #34d399 0%, #10b981 100%);
  border-bottom: 2rpx solid var(--border-color);
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.05);
  border-radius: 24rpx;
  opacity: 1;
  transform: translateY(0);
  transition: all 0.3s ease-in-out;
}

.header-content {
  position: relative;
  z-index: 1;
  text-align: center;
}

.header-title {
  display: block;
  font-size: 48rpx;
  font-weight: 600;
  color: #ffffff;
  margin-bottom: 16rpx;
  text-shadow: 0 1rpx 2rpx rgba(0, 0, 0, 0.1);
}

.header-subtitle {
  display: block;
  font-size: 26rpx;
  color: rgba(255, 255, 255, 0.9);
  text-shadow: 0 1rpx 2rpx rgba(0, 0, 0, 0.1);
}

/* 卡片基础样式 */
.card {
  background: var(--card-bg);
  border-radius: 24rpx;
  margin: 0 30rpx 30rpx;
  padding: 30rpx;
  box-shadow: 0 4rpx 20rpx var(--shadow-color);
  transform: translateY(0);
  opacity: 1;
  transition: all 0.3s ease;
}



.card-header {
  display: flex;
  align-items: center;
  margin-bottom: 24rpx;
}

.card-icon {
  width: 48rpx;
  height: 48rpx;
  font-size: 36rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 16rpx;
}

.card-title {
  flex: 1;
  font-size: 32rpx;
  font-weight: 600;
  color: var(--text-primary);
}

.card-subtitle {
  font-size: 24rpx;
  color: var(--text-tertiary);
}

/* 视图切换卡片 */
.view-switch-card {
  margin: 30rpx 30rpx 30rpx;
  position: relative;
  z-index: 2;
  padding: 20rpx;
}

.view-switch {
  display: flex;
  background: var(--bg-color);
  border-radius: 16rpx;
  padding: 6rpx;
}

.switch-item {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 16rpx 0;
  border-radius: 12rpx;
  transition: all 0.3s ease;
}

.switch-item:active {
  transform: scale(0.98);
}

.switch-item.active {
  background: var(--primary-color);
  box-shadow: 0 4rpx 12rpx rgba(7, 193, 96, 0.3);
}

.switch-icon {
  font-size: 28rpx;
  margin-right: 8rpx;
}

.switch-text {
  font-size: 28rpx;
  font-weight: 500;
  color: var(--text-secondary);
}

.switch-item.active .switch-text {
  color: #ffffff;
}

/* 导航卡片 */
.nav-card {
  padding: 20rpx;
}

.week-header, .month-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.nav-btn {
  display: flex;
  align-items: center;
  padding: 16rpx 20rpx;
  background: var(--bg-color);
  border-radius: 12rpx;
  transition: all 0.2s ease;
}

.nav-btn:active {
  background: #e8e8e8;
  transform: scale(0.95);
}

.nav-icon {
  font-size: 32rpx;
  color: var(--text-secondary);
  font-weight: 600;
}

.nav-text {
  font-size: 26rpx;
  color: var(--text-secondary);
  margin: 0 8rpx;
}

.week-title-container, .month-title-container {
  flex: 1;
  display: flex;
  justify-content: center;
  align-items: center;
}

.week-title-badge, .month-title-badge {
  display: flex;
  align-items: center;
  padding: 16rpx 24rpx;
  background: var(--bg-color);
  border-radius: 16rpx;
  transition: all 0.3s ease;
}

.week-title-badge.current, .month-title-badge.current {
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
}

.week-title, .month-title {
  font-size: 30rpx;
  font-weight: 600;
  color: var(--text-primary);
}

.week-title-badge.current .week-title,
.month-title-badge.current .month-title {
  color: #ffffff;
}

.current-tag {
  font-size: 20rpx;
  color: #ffffff;
  background: rgba(255, 255, 255, 0.3);
  padding: 4rpx 12rpx;
  border-radius: 8rpx;
  margin-left: 12rpx;
}

/* 周排班列表卡片 */
.week-list-card {
  padding: 20rpx;
}

.week-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.day-item {
  display: flex;
  border-radius: 16rpx;
  overflow: hidden;
  background: #fefefe;
  transition: all 0.2s ease;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.04);
}

.day-item:active {
  transform: scale(0.98);
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.08);
}

.day-item.today {
  box-shadow: 0 4rpx 16rpx rgba(7, 193, 96, 0.2);
}

.day-left {
  width: 60rpx;
  padding: 20rpx 8rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background: #fefefe;
  border-right: 1rpx solid var(--border-color);
}

.day-item.today .day-left {
  background: var(--primary-color);
}

.day-weekday {
  font-size: 24rpx;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 6rpx;
}

.day-item.today .day-weekday {
  color: #ffffff;
}

.day-date {
  font-size: 18rpx;
  color: var(--text-tertiary);
}

.day-item.today .day-date {
  color: rgba(255, 255, 255, 0.9);
}

.today-dot {
  width: 8rpx;
  height: 8rpx;
  background: #ffffff;
  border-radius: 50%;
  margin-top: 8rpx;
}

.day-right {
  flex: 1;
  padding: 16rpx;
  display: flex;
  align-items: center;
}

/* 班次信息卡片 */
.shift-info-card {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20rpx 24rpx;
  border-radius: 12rpx;
  border: 2rpx solid;
  gap: 20rpx;
}

.shift-name {
  font-size: 32rpx;
  font-weight: 600;
  color: #000000;
  flex-shrink: 0;
}

.shift-time {
  font-size: 24rpx;
  color: #000000;
  flex: 1;
  text-align: center;
}

.shift-hours-badge {
  display: flex;
  align-items: baseline;
  padding: 12rpx 20rpx;
  border-radius: 24rpx;
  margin-left: 16rpx;
}

.hours-num {
  font-size: 32rpx;
  font-weight: 700;
  color: #ffffff;
}

.hours-unit {
  font-size: 20rpx;
  color: rgba(255, 255, 255, 0.9);
  margin-left: 4rpx;
}

/* 无排班 */
.no-shift {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 20rpx;
}

.no-shift-icon {
  font-size: 40rpx;
  margin-right: 12rpx;
}

.no-shift-text {
  font-size: 28rpx;
  color: var(--text-tertiary);
}

/* 工时统计卡片 */
.stats-card {
  padding: 24rpx;
}

.stats-content {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.stat-row {
  display: flex;
  align-items: center;
}

.stat-row.primary {
  justify-content: center;
  padding: 16rpx 0;
}

.stat-avatar {
  width: 64rpx;
  height: 64rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--bg-color);
  border-radius: 50%;
  font-size: 32rpx;
  margin-right: 20rpx;
}

.stat-main {
  display: flex;
  align-items: baseline;
}

.stat-value {
  font-size: 56rpx;
  font-weight: 700;
  color: var(--primary-color);
}

.stat-unit {
  font-size: 28rpx;
  color: var(--primary-color);
  margin-left: 4rpx;
}

.stat-divider {
  font-size: 36rpx;
  color: var(--border-color);
  margin: 0 16rpx;
}

.stat-standard {
  font-size: 36rpx;
  color: var(--text-secondary);
}

/* 进度条 */
.progress-bar-container {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.progress-bar-bg {
  flex: 1;
  height: 12rpx;
  background: var(--bg-color);
  border-radius: 6rpx;
  overflow: hidden;
}

.progress-bar-fill {
  height: 100%;
  border-radius: 6rpx;
  transition: width 0.5s ease;
}

.progress-bar-fill.under {
  background: linear-gradient(90deg, var(--primary-color) 0%, var(--primary-light) 100%);
}

.progress-bar-fill.over {
  background: linear-gradient(90deg, var(--warning-color) 0%, #ffb84d 100%);
}

.progress-text {
  font-size: 22rpx;
  color: var(--text-tertiary);
  white-space: nowrap;
}

/* 差额显示 */
.stat-row.secondary {
  justify-content: center;
  padding: 12rpx 24rpx;
  background: #fefefe;
  border-radius: 12rpx;
}

.diff-label {
  font-size: 26rpx;
  color: var(--text-secondary);
  margin-right: 16rpx;
}

.diff-value {
  font-size: 32rpx;
  font-weight: 600;
}

.diff-value.over {
  color: var(--warning-color);
}

.diff-value.under {
  color: var(--error-color);
}

/* 图片管理卡片 */
.images-card {
  padding: 24rpx;
}

.images-content {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.images-list {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.image-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20rpx;
  background: var(--bg-color);
  border-radius: 12rpx;
  transition: all 0.2s ease;
}

.image-item:active {
  background: #e8e8e8;
}

.image-info {
  display: flex;
  align-items: center;
  flex: 1;
}

.image-icon {
  font-size: 32rpx;
  margin-right: 12rpx;
}

.image-name {
  font-size: 28rpx;
  color: var(--text-primary);
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.image-actions {
  display: flex;
  gap: 12rpx;
}

.action-btn {
  padding: 10rpx 20rpx;
  border-radius: 8rpx;
  font-size: 24rpx;
  transition: all 0.2s ease;
}

.action-btn.view {
  background: #e6f7ff;
  color: #1890ff;
}

.action-btn.view:active {
  background: #bae7ff;
}

.action-btn.delete {
  background: #fff2f0;
  color: #ff4d4f;
}

.action-btn.delete:active {
  background: #ffccc7;
}

/* 空状态 */
.empty-state {
  text-align: center;
  padding: 60rpx 40rpx;
  background: var(--bg-color);
  border-radius: 12rpx;
}

.empty-icon {
  display: block;
  font-size: 64rpx;
  margin-bottom: 16rpx;
  opacity: 0.5;
}

.empty-text {
  display: block;
  font-size: 28rpx;
  color: var(--text-tertiary);
}

/* 添加图片按钮 */
.add-image-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 24rpx;
  background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-light) 100%);
  border-radius: 12rpx;
  transition: all 0.2s ease;
}

.add-image-btn:active {
  transform: translateY(2rpx);
  box-shadow: 0 4rpx 12rpx rgba(7, 193, 96, 0.3);
}

.add-image-btn .btn-icon {
  font-size: 32rpx;
  color: #ffffff;
  margin-right: 8rpx;
}

.add-image-btn .btn-text {
  font-size: 28rpx;
  color: #ffffff;
  font-weight: 500;
}

/* 图片板块内的周导航 */
.nav-card-inside {
  margin-top: 30rpx;
  padding: 20rpx;
  background: var(--bg-color);
  border-radius: 16rpx;
}

/* 月历卡片 */
.calendar-card {
  padding: 20rpx;
}

.weekdays-header {
  display: flex;
  margin-bottom: 16rpx;
}

.weekday-label {
  flex: 1;
  text-align: center;
  padding: 16rpx 0;
  font-size: 24rpx;
  color: var(--text-tertiary);
  font-weight: 500;
}

.month-grid {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.week-row {
  display: flex;
  gap: 12rpx;
}

.calendar-day {
  flex: 1;
  aspect-ratio: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  border-radius: 12rpx;
  background: var(--bg-color);
  transition: all 0.2s ease;
  position: relative;
  overflow: hidden;
}

.calendar-day:active {
  transform: scale(0.95);
}

.calendar-day.today {
  background: #e6f4ea;
  border: 2rpx solid var(--primary-color);
}

.calendar-day.other-month {
  opacity: 0.5;
}

.day-number-wrapper {
  position: absolute;
  top: 8rpx;
  left: 8rpx;
}

.day-number {
  font-size: 24rpx;
  color: var(--text-primary);
  font-weight: 500;
}

.day-number.today-num {
  width: 36rpx;
  height: 36rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--primary-color);
  color: #ffffff;
  border-radius: 50%;
  font-size: 22rpx;
}

.day-shift-tag {
  position: absolute;
  bottom: 8rpx;
  left: 4rpx;
  right: 4rpx;
  padding: 6rpx 4rpx;
  border-radius: 8rpx;
  text-align: center;
}

.day-shift-tag.empty {
  background: transparent;
}

.shift-tag-name {
  font-size: 18rpx;
  font-weight: 600;
  color: #000000;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  display: block;
}

.day-shift-tag.empty .shift-tag-name {
  color: var(--text-tertiary);
  font-size: 16rpx;
}

/* 底部安全区域 */
.safe-area-bottom {
  height: env(safe-area-inset-bottom);
  min-height: 40rpx;
}

/* 模态框 */
.modal-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  z-index: 1000;
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
}

.modal-mask.show {
  opacity: 1;
  visibility: visible;
}

.modal-content {
  position: fixed;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%) scale(0.9);
  width: 85%;
  max-width: 640rpx;
  background: var(--card-bg);
  border-radius: 24rpx;
  z-index: 1001;
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
  box-shadow: 0 20rpx 60rpx rgba(0, 0, 0, 0.2);
  max-height: 80vh;
  display: flex;
  flex-direction: column;
}

.modal-content.show {
  opacity: 1;
  visibility: visible;
  transform: translate(-50%, -50%) scale(1);
}

.modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 30rpx;
  background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-dark) 100%);
  border-radius: 24rpx 24rpx 0 0;
}

.modal-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #ffffff;
}

.modal-close {
  width: 48rpx;
  height: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(255, 255, 255, 0.2);
  border-radius: 50%;
  transition: all 0.2s ease;
}

.modal-close:active {
  background: rgba(255, 255, 255, 0.3);
}

.close-icon {
  font-size: 28rpx;
  color: #ffffff;
}

.modal-body {
  padding: 30rpx;
  overflow-y: auto;
  flex: 1;
}

.modal-section {
  margin-bottom: 30rpx;
}

.modal-section:last-child {
  margin-bottom: 0;
}

.section-title {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 20rpx;
}

/* 班次选择器 */
.shift-picker-container {
  background: var(--bg-color);
  border-radius: 16rpx;
  padding: 20rpx;
  margin-bottom: 20rpx;
}

.picker-item {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0 20rpx;
}

.picker-shift {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 16rpx 32rpx;
  border: 2rpx solid;
  border-radius: 12rpx;
  background: #ffffff;
}

.picker-shift-name {
  font-size: 32rpx;
  font-weight: 600;
  margin-bottom: 8rpx;
}

.picker-shift-time {
  font-size: 24rpx;
  color: var(--text-secondary);
}

/* 当前排班卡片 */
.current-shift-card {
  padding: 30rpx;
  border-radius: 16rpx;
  border: 2rpx solid;
  margin-bottom: 20rpx;
}

.current-shift-info {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.current-shift-name {
  font-size: 36rpx;
  font-weight: 700;
  margin-bottom: 12rpx;
}

.current-shift-time {
  font-size: 28rpx;
  color: var(--text-secondary);
  margin-bottom: 8rpx;
}

.current-shift-hours {
  font-size: 24rpx;
  color: var(--text-tertiary);
}

/* 模态框按钮 */
.modal-btn {
  width: 100%;
  padding: 24rpx 0;
  border-radius: 12rpx;
  font-size: 30rpx;
  font-weight: 600;
  text-align: center;
  transition: all 0.2s ease;
}

.modal-btn.confirm-btn {
  background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-dark) 100%);
  color: #ffffff;
}

.modal-btn.confirm-btn:active {
  transform: scale(0.98);
}

.modal-btn.confirm-btn.disabled {
  background: #e0e0e0;
  color: #999;
}

.modal-btn.remove-btn {
  background: linear-gradient(135deg, #ff4d4f 0%, #cf1322 100%);
  color: #ffffff;
}

.modal-btn.remove-btn:active {
  transform: scale(0.98);
}

.modal-btn.cancel-btn {
  background: var(--bg-color);
  color: var(--text-secondary);
}

.modal-btn.cancel-btn:active {
  background: #e0e0e0;
}

/* 空状态提示 */
.empty-tip {
  text-align: center;
  padding: 80rpx 40rpx;
}

.empty-tip .empty-icon {
  font-size: 80rpx;
  margin-bottom: 20rpx;
}

.empty-tip .empty-text {
  font-size: 30rpx;
  color: var(--text-secondary);
  margin-bottom: 12rpx;
  display: block;
}

.empty-tip .empty-subtext {
  font-size: 24rpx;
  color: var(--text-tertiary);
  display: block;
}

/* 输入框 */
.input-group {
  margin-bottom: 30rpx;
  position: relative;
  z-index: 1;
}

.input-label {
  display: block;
  font-size: 28rpx;
  color: var(--text-primary);
  margin-bottom: 16rpx;
  font-weight: 500;
  position: relative;
  z-index: 1;
}

.modal-input {
  width: 100%;
  height: 88rpx;
  padding: 0 24rpx;
  border: 2rpx solid var(--border-color);
  border-radius: 12rpx;
  font-size: 30rpx;
  color: var(--text-primary);
  background: #ffffff;
  box-sizing: border-box;
  transition: all 0.2s ease;
  position: relative;
  z-index: 2;
}

.modal-input:focus {
  border-color: var(--primary-color);
  background: #ffffff;
}

/* 上传区域 */
.upload-section {
  margin-bottom: 30rpx;
}

.upload-placeholder {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 80rpx 40rpx;
  background: var(--bg-color);
  border: 2rpx dashed var(--border-color);
  border-radius: 16rpx;
  transition: all 0.2s ease;
}

.upload-placeholder:active {
  background: #e8e8e8;
  border-color: var(--primary-color);
}

.upload-icon {
  font-size: 64rpx;
  margin-bottom: 16rpx;
}

.upload-text {
  font-size: 30rpx;
  color: var(--text-primary);
  font-weight: 500;
  margin-bottom: 8rpx;
}

.upload-hint {
  font-size: 24rpx;
  color: var(--text-tertiary);
}

.upload-preview {
  position: relative;
  width: 100%;
  height: 400rpx;
  border-radius: 16rpx;
  overflow: hidden;
}

.preview-image {
  width: 100%;
  height: 100%;
}

.preview-overlay {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  opacity: 0;
  transition: all 0.2s ease;
}

.upload-preview:active .preview-overlay {
  opacity: 1;
}

.overlay-text {
  color: #ffffff;
  font-size: 30rpx;
  font-weight: 500;
  padding: 16rpx 32rpx;
  background: rgba(255, 255, 255, 0.2);
  border-radius: 8rpx;
}

/* 模态框底部 */
.modal-footer {
  display: flex;
  gap: 20rpx;
  padding-top: 20rpx;
}

.modal-footer .modal-btn {
  flex: 1;
}

/* 操作菜单样式 */
.action-menu-content {
  max-height: 50vh;
}

.action-menu-body {
  padding: 20rpx 30rpx 40rpx;
}

.action-menu {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.action-item {
  display: flex;
  align-items: center;
  padding: 30rpx;
  background: var(--bg-color);
  border-radius: 16rpx;
  transition: all 0.2s ease;
}

.action-item:active {
  transform: scale(0.98);
  background: #e8e8e8;
}

.action-item.disabled {
  opacity: 0.5;
  pointer-events: none;
}

.action-item.add {
  background: linear-gradient(145deg, #e6f7ff 0%, #f0f9ff 100%);
  border: 2rpx solid rgba(24, 144, 255, 0.2);
}

.action-item.add:active {
  background: linear-gradient(145deg, #bae7ff 0%, #e6f7ff 100%);
}

.action-item.delete {
  background: linear-gradient(145deg, #fff2f0 0%, #fff5f5 100%);
  border: 2rpx solid rgba(255, 77, 79, 0.2);
}

.action-item.delete:active {
  background: linear-gradient(145deg, #ffccc7 0%, #fff2f0 100%);
}

.action-icon {
  width: 64rpx;
  height: 64rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 36rpx;
  margin-right: 20rpx;
}

.action-info {
  flex: 1;
  display: flex;
  flex-direction: column;
}

.action-title {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 8rpx;
}

.action-item.add .action-title {
  color: #1890ff;
}

.action-item.delete .action-title {
  color: #ff4d4f;
}

.action-desc {
  font-size: 24rpx;
  color: var(--text-tertiary);
}

.action-arrow {
  font-size: 40rpx;
  color: var(--text-tertiary);
  font-weight: 300;
}

/* 底部滚动班次选择器样式 */
.bottom-picker-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  z-index: 1000;
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
}

.bottom-picker-mask.show {
  opacity: 1;
  visibility: visible;
}

.bottom-picker-container {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  background: var(--card-bg);
  border-radius: 24rpx 24rpx 0 0;
  z-index: 1001;
  transform: translateY(100%);
  transition: transform 0.3s ease;
  box-shadow: 0 -10rpx 40rpx rgba(0, 0, 0, 0.1);
}

.bottom-picker-container.show {
  transform: translateY(0);
}

.bottom-picker-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24rpx 30rpx;
  border-bottom: 1rpx solid var(--border-color);
  background: var(--card-bg);
  border-radius: 24rpx 24rpx 0 0;
}

.bottom-picker-title {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--text-primary);
}

.bottom-picker-close-btn {
  display: flex;
  align-items: center;
  padding: 12rpx 24rpx;
  background: #fff2f0;
  border: 2rpx solid rgba(255, 77, 79, 0.2);
  border-radius: 24rpx;
  transition: all 0.2s ease;
}

.bottom-picker-close-btn:active {
  background: #ffccc7;
  border-color: var(--error-color);
}

.close-btn-icon {
  font-size: 24rpx;
  color: var(--error-color);
  margin-right: 8rpx;
}

.close-btn-text {
  font-size: 28rpx;
  color: var(--error-color);
  font-weight: 500;
}

.bottom-picker-actions {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.bottom-picker-delete {
  width: 48rpx;
  height: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: var(--error-color);
  color: #ffffff;
  transition: all 0.2s ease;
}

.bottom-picker-delete:active {
  background: #cf1322;
  transform: scale(0.95);
}

.delete-icon {
  font-size: 32rpx;
  font-weight: 600;
  color: #ffffff;
}

.bottom-picker-confirm {
  width: 48rpx;
  height: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: var(--primary-color);
  color: #ffffff;
  transition: all 0.2s ease;
}

.bottom-picker-confirm:active {
  background: var(--primary-dark);
  transform: scale(0.95);
}

.confirm-icon {
  font-size: 32rpx;
  font-weight: 600;
  color: #ffffff;
}

.bottom-picker-close {
  width: 48rpx;
  height: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  transition: all 0.2s ease;
}

.bottom-picker-close:active {
  background: var(--bg-color);
}

.bottom-picker-content {
  padding: 0;
  height: 350rpx;
}

.shift-picker-view {
  width: 100%;
  height: 100%;
}

.picker-item {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 200rpx;
  padding: 0 24rpx;
}

.picker-item-color {
  width: 20rpx;
  height: 20rpx;
  border-radius: 50%;
  margin-right: 20rpx;
  flex-shrink: 0;
}

.picker-item-name {
  font-size: 48rpx;
  font-weight: 700;
  color: var(--text-primary);
}



/* 响应式适配 */
@media (max-width: 375px) {
  .day-left {
    width: 80rpx;
    padding: 16rpx 8rpx;
  }
  
  .day-weekday {
    font-size: 24rpx;
  }
  
  .day-date {
    font-size: 20rpx;
  }
  
  .shift-name {
    font-size: 28rpx;
  }
  
  .shift-time {
    font-size: 22rpx;
  }
  
  .calendar-day {
    border-radius: 8rpx;
  }
  
  .shift-tag-name {
    font-size: 16rpx;
  }
}

@media (min-width: 768px) {
  .card {
    margin: 0 60rpx 40rpx;
  }
  
  .day-left {
    width: 120rpx;
  }
}

/* ==========================================
   WeUI 风格样式
   ========================================== */

/* WeUI 遮罩层 */
.weui-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.6);
  z-index: 1000;
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
}

.weui-mask.show {
  opacity: 1;
  visibility: visible;
}

/* WeUI 底部弹出面板 */
.weui-sheet {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  background: #f7f7f7;
  z-index: 1001;
  transform: translateY(100%);
  transition: transform 0.3s ease;
  border-radius: 24rpx 24rpx 0 0;
  max-height: 70vh;
  display: flex;
  flex-direction: column;
}

.weui-sheet.show {
  transform: translateY(0);
}

/* WeUI 标题栏 */
.weui-sheet__hd {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 28rpx 30rpx;
  background: #ffffff;
  border-radius: 24rpx 24rpx 0 0;
  border-bottom: 1rpx solid #e5e5e5;
  position: relative;
  z-index: 2;
}

.weui-sheet__action {
  padding: 12rpx 24rpx;
  font-size: 32rpx;
  line-height: 1.5;
}

.weui-sheet__action_default {
  color: #576b95;
}

.weui-sheet__action_primary {
  color: #07c160;
  font-weight: 500;
}

.weui-sheet__title {
  font-size: 32rpx;
  font-weight: 600;
  color: #000000;
  flex: 1;
  text-align: center;
}

/* WeUI 内容区 */
.weui-sheet__bd {
  background: #ffffff;
  flex: 1;
  overflow-y: auto;
  -webkit-overflow-scrolling: touch;
}

/* WeUI 单元格 */
.weui-cells {
  background-color: #ffffff;
  line-height: 1.47058824;
  font-size: 17px;
}

.weui-cells_radio {
  margin-top: 0;
}

.weui-cell {
  padding: 30rpx;
  position: relative;
  display: flex;
  align-items: center;
  transition: background-color 0.2s;
}

.weui-cell:before {
  content: '';
  position: absolute;
  left: 0;
  top: 0;
  right: 0;
  height: 1px;
  border-top: 1rpx solid #e5e5e5;
  color: #e5e5e5;
  left: 30rpx;
}

.weui-cell:first-child:before {
  display: none;
}

.weui-cell:active {
  background-color: #ececec;
}

.weui-cell_access {
  -webkit-tap-highlight-color: rgba(0, 0, 0, 0);
}

.weui-cell_warn {
  color: #fa5151;
}

.weui-cell__bd {
  flex: 1;
}

.weui-cell__ft {
  text-align: right;
  color: #999999;
}

.weui-cell__warn-text {
  color: #fa5151;
  font-size: 32rpx;
}

/* 选中状态 */
.weui-cell_checked {
  background-color: #f7f7f7;
}

.weui-cell_checked .weui-cell__bd {
  color: #07c160;
}

/* 班次标签样式 */
.shift-label {
  display: flex;
  align-items: center;
  margin-bottom: 8rpx;
}

.shift-color-dot {
  width: 24rpx;
  height: 24rpx;
  border-radius: 50%;
  margin-right: 16rpx;
  flex-shrink: 0;
}

.shift-name {
  font-size: 32rpx;
  font-weight: 600;
  color: #000000;
}

.shift-time {
  font-size: 26rpx;
  color: #999999;
  margin-left: 40rpx;
}

/* 安全区域 */
.weui-sheet__safe-area-bottom {
  height: env(safe-area-inset-bottom);
  min-height: 40rpx;
  background: #ffffff;
}
